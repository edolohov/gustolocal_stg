# Следующие шаги для настройки Staging

## ✅ Что уже сделано:
1. База данных импортирована в `u850527203_stg`
2. Все таблицы Wordfence удалены (будут созданы заново при первом запуске)
3. Все AUTO_INCREMENT исправлены в CREATE TABLE

## 📋 Что нужно сделать сейчас:

### Шаг 1: Проверь, установлен ли WordPress на staging

Открой в браузере: `https://staging.gustolocal.es`

**Если видишь ошибку или пустую страницу:**
- Нужно установить WordPress на поддомен
- Или загрузить файлы WordPress в папку `/home/u850527203/domains/gustolocal.es/public_html/staging/`

**Если видишь WordPress (даже с ошибками):**
- Переходи к шагу 2

### Шаг 2: Настрой wp-config.php

1. Зайди на сервер через FTP/File Manager
2. Открой файл `/home/u850527203/domains/gustolocal.es/public_html/staging/wp-config.php`
3. Измени следующие строки:

```php
// База данных
define( 'DB_NAME', 'u850527203_stg' );
define( 'DB_USER', 'u850527203' );
define( 'DB_PASSWORD', 'hiLKov15!' );
define( 'DB_HOST', 'localhost' );

// Префикс таблиц (ВАЖНО: staging_, а не stg_!)
$table_prefix = 'staging_';

// URL для staging
define('WP_HOME', 'https://staging.gustolocal.es');
define('WP_SITEURL', 'https://staging.gustolocal.es');
```

### Шаг 3: Замени URL в базе данных

**Вариант А: Используй скрипт (рекомендуется)**

1. Загрузи файл `docs/search-replace-urls.php` на сервер в папку staging
2. Открой в браузере: `https://staging.gustolocal.es/search-replace-urls.php?key=CHANGE_THIS_TO_SOMETHING_SECRET`
   - **ВАЖНО:** Замени `CHANGE_THIS_TO_SOMETHING_SECRET` на свой секретный ключ в файле перед загрузкой!
3. После выполнения удали файл с сервера!

**Вариант Б: Выполни SQL запросы вручную**

Зайди в phpMyAdmin → база `u850527203_stg` → вкладка SQL и выполни:

```sql
-- Замена в options
UPDATE staging_options SET option_value = REPLACE(option_value, 'https://gustolocal.es', 'https://staging.gustolocal.es') WHERE option_name IN ('siteurl', 'home');
UPDATE staging_options SET option_value = REPLACE(option_value, 'https://gustolocal.es', 'https://staging.gustolocal.es');

-- Замена в posts
UPDATE staging_posts SET post_content = REPLACE(post_content, 'https://gustolocal.es', 'https://staging.gustolocal.es');
UPDATE staging_posts SET guid = REPLACE(guid, 'https://gustolocal.es', 'https://staging.gustolocal.es');

-- Замена в postmeta
UPDATE staging_postmeta SET meta_value = REPLACE(meta_value, 'https://gustolocal.es', 'https://staging.gustolocal.es') WHERE meta_value LIKE '%gustolocal.es%';
```

### Шаг 4: Проверь работу сайта

1. Открой `https://staging.gustolocal.es` — должен открыться сайт
2. Открой `https://staging.gustolocal.es/wp-admin/` — должен открыться вход в админку
3. Войди с теми же логином и паролем, что и на production

### Шаг 5: Отключи production интеграции

В админке WordPress отключи или настрой тестовый режим для:
- Платежных систем (Stripe, PayPal)
- Email уведомлений
- Других внешних сервисов

## 🆘 Если что-то не работает:

1. **Сайт не открывается:**
   - Проверь, что WordPress установлен в папке staging
   - Проверь права доступа к файлам (должны быть 644 для файлов, 755 для папок)

2. **Ошибка подключения к базе данных:**
   - Проверь настройки в wp-config.php
   - Проверь, что база данных `u850527203_stg` существует

3. **Сайт открывается, но показывает production URL:**
   - Выполни замену URL (шаг 3)
   - Проверь настройки WP_HOME и WP_SITEURL в wp-config.php

4. **Ошибки в админке:**
   - Включи WP_DEBUG в wp-config.php (уже должно быть включено)
   - Проверь файл `wp-content/debug.log`

## 📝 После успешной настройки:

1. Удали скрипт `search-replace-urls.php` с сервера
2. Проверь, что все плагины работают
3. Начни тестировать изменения на staging

