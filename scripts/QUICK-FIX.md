# 🚨 Быстрое исправление Staging (HTTP 500)

## Что я проверил:

1. ✅ Синтаксис PHP файлов - все в порядке
2. ✅ wp-config.php - конфигурация выглядит правильно
3. ✅ Создан комплексный диагностический скрипт

## Что нужно сделать СЕЙЧАС:

### 1. Загрузите диагностический скрипт на сервер

```bash
cd /Users/eugene/Documents/site-repo/gustolocal-stg/
scp -P 65002 scripts/full-staging-check.php u850527203@82.29.185.42:/home/u850527203/domains/gustolocal.es/public_html/staging/
```

### 2. Откройте скрипт в браузере

```
https://staging.gustolocal.es/full-staging-check.php
```

### 3. Скрипт покажет точную проблему

Скрипт проверит:
- Подключение к БД (проверит оба варианта пользователя: `u850527203` и `u850527203_stg`)
- Наличие таблиц в БД
- Настройки wp-config.php
- Синтаксис PHP
- Ошибки в debug.log
- Попытку загрузить WordPress

## Возможные проблемы:

### Если проблема с БД:
- Проверьте, что база `u850527203_stg` существует
- Проверьте пользователя БД в wp-config.php на сервере
- Возможно нужно изменить `DB_USER` с `u850527203_stg` на `u850527203` (или наоборот)

### Если проблема с файлами:
- Убедитесь, что все файлы загружены на сервер
- Проверьте права доступа (644 для файлов, 755 для папок)

### Если проблема с темой:
- Проверьте, что тема `gustolocal` существует в `wp-content/themes/`
- Проверьте синтаксис `functions.php` темы

## После исправления:

1. Удалите `full-staging-check.php` с сервера
2. Проверьте работу сайта

