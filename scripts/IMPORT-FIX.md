# 🔧 Исправление проблемы с импортом БД

## Проблема
При импорте БД возникает ошибка, что таблицы уже существуют, даже после выполнения скрипта очистки.

## Решение

### Вариант 1: Использовать простой скрипт очистки (РЕКОМЕНДУЕТСЯ)

1. **Выполните скрипт `clear-staging-db-simple.sql`:**
   - Откройте phpMyAdmin
   - Выберите базу данных `u850527203_stg`
   - Перейдите на вкладку **SQL**
   - Скопируйте и выполните содержимое файла `scripts/clear-staging-db-simple.sql`
   - Этот скрипт удалит все таблицы, включая `stg_custom_feedback_requests`

2. **Проверьте, что все таблицы удалены:**
   ```sql
   SELECT COUNT(*) as remaining_tables 
   FROM information_schema.TABLES 
   WHERE TABLE_SCHEMA = 'u850527203_stg' 
   AND (TABLE_NAME LIKE 'stg_%' OR TABLE_NAME LIKE 'staging_%');
   ```
   Должно вернуть `0`

3. **Теперь импортируйте БД:**
   - Экспортируйте из `u850527203_5vYEq` (production)
   - Импортируйте в `u850527203_stg` (staging)
   - При импорте выберите опцию **"Добавить DROP TABLE"** если она есть

### Вариант 2: Использовать динамический скрипт

Если простой скрипт не сработал, используйте `clear-staging-db-dynamic.sql`:
- Этот скрипт динамически найдет и удалит ВСЕ таблицы с префиксом `stg_` или `staging_`

### Вариант 3: Ручное удаление через phpMyAdmin

1. В phpMyAdmin выберите базу данных `u850527203_stg`
2. Перейдите на вкладку **Структура**
3. Отметьте **ВСЕ** таблицы (галочка вверху)
4. В выпадающем меню выберите **Удалить**
5. Подтвердите удаление

---

## После успешного импорта

### Шаг 1: Переименовать таблицы

Выполните скрипт `rename-tables-stg-to-staging.sql` или `rename-tables-dynamic.sql`

**Рекомендуется использовать динамический скрипт** (`rename-tables-dynamic.sql`), так как он автоматически найдет и переименует все таблицы.

### Шаг 2: Заменить URL

Выполните скрипт `replace-urls-in-db.sql`

---

## Проверка после импорта

```sql
-- Проверяем, что все таблицы переименованы
SELECT TABLE_NAME 
FROM information_schema.TABLES 
WHERE TABLE_SCHEMA = 'u850527203_stg' 
AND TABLE_NAME LIKE 'staging_%'
ORDER BY TABLE_NAME;

-- Проверяем URL
SELECT option_name, option_value 
FROM staging_options 
WHERE option_name IN ('siteurl', 'home');
```

---

## Если таблицы все равно не удаляются

1. **Проверьте права доступа:**
   - Убедитесь, что пользователь `u850527203_stg` имеет права на удаление таблиц

2. **Попробуйте удалить через командную строку:**
   ```bash
   mysql -h localhost -u u850527203_stg -p'hiLKov15!' u850527203_stg < scripts/clear-staging-db-simple.sql
   ```

3. **Или удалите базу данных и создайте заново:**
   ```sql
   DROP DATABASE IF EXISTS u850527203_stg;
   CREATE DATABASE u850527203_stg CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_520_ci;
   ```
   Затем импортируйте БД заново.

