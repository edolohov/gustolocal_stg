# 📋 Инструкция по импорту БД с переименованием префикса

## Проблема 1: Таблицы уже существуют
**Решение:** Сначала нужно очистить staging БД

## Проблема 2: Переименование префикса с `stg_` на `staging_`
**Решение:** После импорта выполнить скрипт переименования

---

## 🔄 Пошаговая инструкция:

### Шаг 1: Очистить staging БД

1. Войдите в phpMyAdmin
2. Выберите базу данных `u850527203_stg`
3. Перейдите на вкладку **SQL**
4. Откройте файл `scripts/clear-staging-db.sql` (локально)
5. Скопируйте весь SQL код из файла
6. Вставьте в поле SQL в phpMyAdmin
7. Нажмите **Вперед**
8. Убедитесь, что все таблицы удалены

**Или через командную строку:**
```bash
cd /Users/eugene/Documents/site-repo/gustolocal-stg/
mysql -h localhost -u u850527203_stg -p'hiLKov15!' u850527203_stg < scripts/clear-staging-db.sql
```

---

### Шаг 2: Импортировать БД из production

1. В phpMyAdmin выберите базу данных `u850527203_5vYEq` (production)
2. Перейдите на вкладку **Экспорт**
3. Выберите метод **Обычный**
4. В разделе **Структура**:
   - ✅ Отметьте **Добавить DROP TABLE / DROP VIEW**
   - ✅ Отметьте **Добавить CREATE TABLE**
5. В разделе **Данные**:
   - ✅ Отметьте **Добавить INSERT**
6. Нажмите **Вперед** и скачайте SQL дамп
7. Выберите базу данных `u850527203_stg` (staging)
8. Перейдите на вкладку **Импорт**
9. Загрузите скачанный SQL файл
10. Нажмите **Вперед**

**Важно:** После импорта таблицы будут с префиксом `stg_` (как в production)

---

### Шаг 3: Переименовать таблицы с `stg_` на `staging_`

1. В phpMyAdmin выберите базу данных `u850527203_stg`
2. Перейдите на вкладку **SQL**
3. Откройте файл `scripts/rename-tables-stg-to-staging.sql` (локально)
4. Скопируйте весь SQL код из файла
5. Вставьте в поле SQL в phpMyAdmin
6. Нажмите **Вперед**
7. Убедитесь, что все таблицы переименованы (проверьте список таблиц)

**Или через командную строку:**
```bash
cd /Users/eugene/Documents/site-repo/gustolocal-stg/
mysql -h localhost -u u850527203_stg -p'hiLKov15!' u850527203_stg < scripts/rename-tables-stg-to-staging.sql
```

---

### Шаг 4: Заменить URL в базе данных

1. В phpMyAdmin выберите базу данных `u850527203_stg`
2. Перейдите на вкладку **SQL**
3. Откройте файл `scripts/replace-urls-in-db.sql` (локально)
4. Скопируйте весь SQL код из файла
5. Вставьте в поле SQL в phpMyAdmin
6. Нажмите **Вперед**
7. Проверьте результат - должны быть URL `https://staging.gustolocal.es`

**Или через командную строку:**
```bash
cd /Users/eugene/Documents/site-repo/gustolocal-stg/
mysql -h localhost -u u850527203_stg -p'hiLKov15!' u850527203_stg < scripts/replace-urls-in-db.sql
```

---

## ✅ Проверка результата

После выполнения всех шагов проверьте:

1. **Таблицы переименованы:**
   ```sql
   SELECT TABLE_NAME FROM information_schema.TABLES 
   WHERE TABLE_SCHEMA = 'u850527203_stg' 
   AND TABLE_NAME LIKE 'staging_%'
   ORDER BY TABLE_NAME;
   ```
   Должны быть таблицы с префиксом `staging_`

2. **URL заменены:**
   ```sql
   SELECT option_name, option_value 
   FROM staging_options 
   WHERE option_name IN ('siteurl', 'home');
   ```
   Должны быть URL `https://staging.gustolocal.es`

3. **wp-config.php обновлен:**
   - Откройте `wp-config.php` на сервере
   - Убедитесь, что `$table_prefix = 'staging_';`

---

## 🆘 Если что-то пошло не так

**Ошибка при переименовании:**
- Убедитесь, что импорт завершен успешно
- Проверьте, что все таблицы с префиксом `stg_` существуют

**Таблицы не переименовались:**
- Выполните скрипт переименования еще раз
- Проверьте, что нет опечаток в названиях таблиц

**URL не заменились:**
- Выполните скрипт замены URL еще раз
- Убедитесь, что используете правильный префикс таблиц (`staging_`)

