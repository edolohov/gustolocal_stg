# ✅ Статус настройки Staging окружения

## 🎉 Что уже сделано автоматически:

### ✅ Структура проекта
- [x] Скопирована структура из production (`wp-content`, `weekly-meal-builder`, `twentytwentyfour`, `woocommerce-templates`)
- [x] Скачан WordPress core (последняя версия)
- [x] Создана структура папок

### ✅ Git репозиторий
- [x] Git инициализирован
- [x] Remote настроен на `https://github.com/edolohov/gustolocal_stg`
- [x] Ветка переименована в `main`

### ✅ Конфигурационные файлы
- [x] `wp-config.php` создан с настройками для staging
- [x] `.htaccess` создан с правилами для staging
- [x] `.gitignore` настроен (исключает `.env.local`, `wp-config.php`)

### ✅ Скрипты
- [x] `scripts/deploy-to-staging.sh` - деплой на staging
- [x] `scripts/db-connect.sh` - подключение к БД (staging/production)
- [x] `scripts/wp-cli.sh` - выполнение WP-CLI команд
- [x] `scripts/git-commit.sh` - коммит в Git
- [x] `scripts/load-env.sh` - загрузка переменных окружения
- [x] `scripts/deactivate-wordfence.sh` - отключение Wordfence
- [x] `scripts/replace-urls-in-db.sql` - SQL скрипт для замены URL

### ✅ Документация
- [x] `STAGING-SETUP.md` - полная инструкция по настройке
- [x] `QUICK-START.md` - краткий план действий
- [x] `MANUAL-STEPS.md` - шаги, которые нужно выполнить вручную
- [x] `README.md` - обзор проекта

---

## 📋 Что нужно сделать ВРУЧНУЮ:

### 1. Создать `.env.local` ⚠️ ОБЯЗАТЕЛЬНО

Создайте файл `.env.local` в корне проекта. См. инструкцию в `MANUAL-STEPS.md`, пункт 1.

**Важно:** Замените `ВАШ_ПАРОЛЬ_ПРОДА_ЗДЕСЬ` на реальный пароль от production БД.

---

### 2. Загрузить файлы на сервер ⚠️ ОБЯЗАТЕЛЬНО

Загрузите все файлы на сервер в папку `/home/u850527203/domains/gustolocal.es/public_html/staging/`

**Команда для rsync:**
```bash
cd /Users/eugene/Documents/site-repo/gustolocal-stg/
rsync -avz -e "ssh -p 65002" \
  --exclude '.git' \
  --exclude '.env.local' \
  --exclude '*.md' \
  --exclude 'docs/' \
  . \
  u850527203@82.29.185.42:/home/u850527203/domains/gustolocal.es/public_html/staging/
```

Или используйте FileZilla/SFTP клиент.

---

### 3. Клонировать базу данных ⚠️ ОБЯЗАТЕЛЬНО

Экспортируйте БД из production (`u850527203_5vYEq`) и импортируйте в staging (`u850527203_stg`).

См. подробную инструкцию в `MANUAL-STEPS.md`, пункт 3.

---

### 4. Заменить URL в базе данных ⚠️ ОБЯЗАТЕЛЬНО

После импорта БД выполните SQL скрипт `scripts/replace-urls-in-db.sql`.

См. инструкцию в `MANUAL-STEPS.md`, пункт 4.

---

### 5. Настроить домен ⚠️ ОБЯЗАТЕЛЬНО

В панели управления хостингом настройте поддомен `staging.gustolocal.es` на папку `/public_html/staging`.

См. инструкцию в `MANUAL-STEPS.md`, пункт 5.

---

### 6. Отключить Wordfence ⚠️ РЕКОМЕНДУЕТСЯ

Отключите Wordfence и другие ненужные плагины.

См. инструкцию в `MANUAL-STEPS.md`, пункт 6.

---

### 7. Проверить работу ⚠️ ОБЯЗАТЕЛЬНО

Откройте https://staging.gustolocal.es и проверьте, что всё работает.

См. инструкцию в `MANUAL-STEPS.md`, пункт 7.

---

### 8. Первый коммит в Git

```bash
cd /Users/eugene/Documents/site-repo/gustolocal-stg/
git add .
git commit -m "Initial staging setup"
git push -u origin main
```

---

## 📚 Документация

- **`MANUAL-STEPS.md`** - пошаговые инструкции для ручных действий
- **`STAGING-SETUP.md`** - полная документация по настройке
- **`QUICK-START.md`** - краткий план действий
- **`README.md`** - обзор проекта

---

## 🎯 Следующие шаги

1. Откройте файл `MANUAL-STEPS.md`
2. Выполните все шаги по порядку
3. После завершения удалите пароли из `STAGING-SETUP.md`

---

**Текущий статус:** ✅ Локальная подготовка завершена, требуется загрузка на сервер и настройка БД

